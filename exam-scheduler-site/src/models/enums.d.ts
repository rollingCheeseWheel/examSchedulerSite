export enum AuditLogActor {
    Student,
    Teacher,
    Admin,
    System
}

export enum AutoLockIn {
    FixedDate,
    TimeBeforeExamination
}

export enum UserRole {
    Student = 0,
    Teacher = 1,
    Admin = 2
}